# 指令列表

---

::: details 点击此处查看本页目录
[[toc]]
:::

::: warning
部分指令需要权限才能使用

此文档可能不是最新版，建议使用 `helps` 指令查询最新版文档
:::

::: danger 特别提醒
- 除非有特殊说明，否则以下的所有指令都要用前缀调用：lar、📢📢
- 也可以 @Bot + 指令
- 大部分指令需要 空格 分隔使用，如：`前缀` `指令` `参数(可选)`
:::

---

## 群管理
| 指令 | 说明 |
|------|------|
| `上管` | 上管 @群友 |
| `下管` | 下管 @群友 |
| `禁言` | 禁言 `<秒数>` @群友 |
| `解禁` | 解禁 @群友 |
| `踢了` | 踢了 @群友 |
| `拉黑` | 拉黑 @群友 |
| `改名` | 改名 `<新昵称>` @群友 |
| `改我` | 改我 `<新昵称>` |
| `取名` | 取名 @群友 `<消息轮数>` |
| `取头衔` | 取头衔 @群友 `<消息轮数>` |
| `头衔` | 头衔 `<头衔内容>` @群友 |
| `申请头衔` | 申请头衔 `<头衔内容>` |
| `撤回` | [引用消息]撤回<br>或 撤回 @群友 `<消息轮数>` |
| `设置禁词` | 禁词 + 词1 词2，+为添加，-为删除，不带参数删除词 |
| `内置禁词` | 内置禁词 开/关 |
| `禁词禁言` | 禁词禁言 `<秒数>`，设为 0 表示关闭禁词检测 |
| `刷新禁言` | 刷新禁言 `<秒数>`，设为 0 表示关闭禁言检测 |
| `投票禁言` | 投票禁言 `<秒数>` @群友 |
| `赞同禁言` | 同意执行当前禁言投票 |
| `反对禁言` | 反对执行当前禁言投票 |
| `禁我` | 禁我 `<秒数>` |
| `设置群名` | 设置群名 `<新群名>` |
| `设置群头像` | [引用图片]设置群头像 |
| `发布群公告` | [引用图片]发布群公告 `<公告内容>` |
| `查看群公告` | 查看群公告 |
| `上传群文件` | 上传群文件 `<文件夹名/文件名>` 或 `<文件名>` |
| `查看群文件` | 查看群文件 `<文件夹名/序号>` `<文件名/序号>` |
| `删除群文件` | 删除群文件 `<文件夹名/序号>` `<文件名/序号>` |
| `清理群友` | 清理群友 `<未发言天数>` `<群等级>` |
| `群友信息` | 查看群友信息 |
| `群管帮助` | 查看群管帮助 |
| `群管配置` | 群管配置 `<群号>` `<配置项>` `<配置值>` |
| `群管重置` | 群管重置 `<群号>` 或 `all` |
| `移精` | [引用消息]移精 |
| `设精` | [引用消息]设精 |

### 进群管理
| 指令 | 说明 |
|------|------|
| `进群审核` | 进群审核 开/关，所有进群审核的群员开关 |
| `批准` | 批准进群申请 |
| `驳回` | 驳回进群申请 |
| `进群白词` | 设置/查看自动批准进群的关键词（空格隔开，无参数表示查看） |
| `进群黑词` | 设置/查看进群黑名单关键词（空格隔开，无参数表示查看） |
| `进群黑名单` | 进群黑名单 +QQ -QQ，+为新增，-为删除，不带参数视为覆写 |
| `进群次数` | 设置/查看未命中进群关键词多少次后拉黑（0 表示不限制，无参数表示查看） |
| `进群等级` | 设置/查看本群进群等级门槛（0 表示不限制，无参数表示查看） |
| `进群禁言` | 进群禁言 `<秒数>`，设为 0 表示本群不启用该功能 |
| `未命中驳回` | 未命中自动驳回 开/关 |
| `退群拉黑` | 退群拉黑 开/关，拉黑后下次进群直接自动拒绝 |
| `退群通知` | 退群通知 开/关 |
| `进群欢迎` | 进群欢迎 |
| `开启全禁` | 开启全禁 |
| `关闭全禁` | 关闭禁言 |

---

## 手办化插件
| 指令 | 说明 |
|------|------|
| `上下文状态` | 查看上下文状态（管理员） |
| `人设参考图查看` | 查看人设参考图（管理员） |
| `人设参考图添加` | 添加人设参考图（管理员） |
| `人设参考图删除` | 清除所有人设参考图（管理员） |
| `人设场景列表` | 查看所有人设场景 |
| `人设拍照` | 生成人设角色的日常照片（指令模式） |
| `人设状态` | 查看人设功能状态（管理员） |
| `打包PDF` | 将上下文或当前消息中的图片打包为 PDF（不生成新图，纯打包） |
| `测试智能判断` | 测试智能判断功能（不实际生成） |
| `清除上下文` | 清除当前会话的上下文（管理员） |
| `预设参考图列表` | 列出所有预设的参考图（管理员） |
| `预设参考图删除` | 删除预设的参考图（管理员） |
| `预设参考图查看` | 查看预设的参考图（管理员） |
| `预设参考图添加` | 为预设添加参考图（管理员） |
| `预设参考图统计` | 查看预设参考图统计（管理员） |

---

## 内置命令扩展
| 指令 | 说明 |
|------|------|
| `del` | 删除当前对话 |
| `deop` | 取消授权管理员。deop `<admin_id>` |
| `groupnew` | 创建新群聊对话 |
| `history` | 查看对话记录 |
| `llm` | 开启/关闭 LLM |
| `ls` | 查看对话列表 |
| `model` | 查看或切换模型 |
| `op` | 授权管理员。op `<admin_id>` |
| `persona` | 查看或切换 Persona |
| `provider` | 查看或切换 LLM Provider |
| `rename` | 重命名对话 |
| `switch` | 通过 ls 前面的序号切换对话 |
| `plugin` | 插件管理 |
| `get` | 安装插件 |
| `help` | 获取插件帮助 |
| `off` | 禁用插件 |
| `on` | 启用插件 |

---

## QQ 空间
| 指令 | 说明 |
|------|------|
| `写说说` | 写说说 `<主题>` `<图片>`，由 AI 写完后直接发布 |
| `发说说` | 发说说 `<内容>` `<图片>`，由用户指定内容 |
| `看说说` | 看说说 `<@群友>` `<序号>` |
| `删说说` | 删说说 `<说说 ID>` |
| `赞说说` | 赞说说 `<序号/范围>` |
| `评说说` | 评说说 `<序号/范围>` |
| `回评` | 回评 `<说说 ID>` `<评论内容>`，默认回复最后一条非自己评论 |
| `投稿` | 投稿 `<内容>` `<图片>` |
| `匿名投稿` | 匿名投稿 `<内容>` `<图片>` |
| `看稿` | 查看稿件 `<稿件 ID>`，默认最新稿件 |
| `过稿` | 通过稿件 `<稿件 ID>` |
| `拒稿` | 拒绝稿件 `<稿件 ID>` `<原因>` |
| `撤稿` | 删除稿件 `<稿件 ID>` |
| `查看访客` | 查看访客 |

---

## 人际关系管理
| 指令 | 说明 |
|------|------|
| `好友列表` | 查看 bot 的所有好友信息 |
| `加好友` | 加好友 `<QQ号>` 或 `@某人` [验证消息] [备注] |
| `删好友` | 删好友 `@群友` 或 `<QQ号>` 或 `<昵称>` [可批量] |
| `同意` | 同意好友申请或群邀请 |
| `拒绝` | 拒绝好友申请或群邀请 |
| `推荐` | 推荐 `@群友` 或 `@qq` |
| `群列表` | 查看 bot 加入的所有群信息 |
| `加群` | 加群 [群号] [答案] |
| `退群` | 退群 `@群友` 或 `<群号>` 或 `<区间>` [可批量] |
| `加审批员` | 加审批员 @某人 |
| `减审批员` | 减审批员 @某人 |
| `抽查` | 抽查 [群友] 或 [@群友] 或 [QQ号] [数量]，抽查聊天记录 |

---

## 广播助手
| 指令 | 说明 |
|------|------|
| `广播` | [引用消息]广播 `群聊` 或 `私聊` 或 `全部` |
| `取消广播` | 取消当前正在进行的广播任务 |
| `广播列表` | 广播列表 [留空] 或 `群聊` 或 `私聊` |
| `开启广播` | 开启广播 [留空] 或 `群聊` 或 `私聊` `<序号>` |
| `关闭广播` | 关闭广播 [留空] 或 `群聊` 或 `私聊` `<序号>` |

---

## 重启插件
| 指令 | 说明 |
|------|------|
| `重启` | 重启 AstrBot |
| `重载` | 重载 `<插件名>` 或 `<序号>` 或 `all` |
| `定时重启` | 定时重启 开/关 |

---

## 更新管理
| 指令 | 说明 |
|------|------|
| `更新所有插件` | 检查并更新所有插件 |
| `重启astrbot` | 重启 AstrBot |

---

## LLM 插件桥接
| 指令 | 说明 |
|------|------|
| `lpb_config` | 显示当前插件配置 |
| `lpb_info` | 查看指令详情 |
| `lpb_list` | 列出所有可用指令 |
| `lpb_refresh` | 手动刷新缓存 |
| `lpb_wake` | 查看唤醒词配置 |

---

## GitHub 监控
| 指令 | 说明 |
|------|------|
| `github_monitor` | 手动触发监控检查 |
| `github_status` | 查看监控状态 |

---

## 语音转化
| 指令 | 说明 |
|------|------|
| `转语音` | 文件、文本 → 语音 |
| `转文件` | 语音 → 文件 |

---

## 黑名单管理
| 指令 | 说明 |
|------|------|
| `ban` | 在会话中禁用指定用户的使用权限 |
| `ban-all` | 在全局禁用指定用户的使用权限 |
| `ban-enable` | 启用禁用功能 |
| `ban-disable` | 停用禁用功能 |
| `ban-help` | 显示 reneban 帮助信息 |
| `ban-reset` | 删除一名禁用用户的所有记录 |
| `banlist` | 显示当前禁用用户名单 |
| `pass` | 在会话中解禁指定用户 |
| `pass-all` | 在全局解禁指定用户 |
| `dec-ban` | 删除指定用户的会话禁用时间 |
| `dec-ban-all` | 删除指定用户的全部禁用时间 |
| `dec-pass` | 删除指定用户的会话解禁时间 |
| `dec-pass-all` | 删除指定用户的全部解禁时间 |

---

## 多类型消息发送
| 指令 | 说明 |
|------|------|
| `at` | at @某人 或 `all`（@全体） |
| `face` | face `<表情ID>` `<数量>` |
| `forward` | 转发引用的消息 |
| `markdown` | 发送 markdown 消息 |
| `music` | music `<平台>` `<歌曲ID>` |
| `node` | [引用消息]node `<标题>` `<内容>` |
| `rps` | 猜拳（石头剪刀布） |
| `text` | text `<文本>` |
| `dice` | 骰子 |

---

## 群分析总结
| 指令 | 说明 |
|------|------|
| `群分析` | 分析群日常活动（群平台支持） |
| `分析设置` | 管理分析设置（群平台支持） |
| `增量状态` | 查看当前增量分析状态（滑动窗口） |
| `查看模板` | 查看所有可用的报告模板及预览图（群平台支持） |
| `设置格式` | 设置分析报告输出格式（群平台支持） |
| `设置模板` | 设置分析报告模板（群平台支持） |

---

## 万能解析器
| 指令 | 说明 |
|------|------|
| `开启解析` | 开启当前会话的解析 |
| `关闭解析` | 关闭当前会话的解析 |
| `登录B站` | 扫码登录 B 站 |

---

## 元数据解析
| 指令 | 说明 |
|------|------|
| `raw` | 查看消息的原始结构 |
| `解析` | 解析媒体的信息 |
